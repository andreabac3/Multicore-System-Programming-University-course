This Java Class compute the sum of Array of int. </br>
Computational cost is O(log n) and not O(n).  </br>
Because we use the ForkJoin Library which uses parallelism. </br>
HOW TO USE: Use 'Principale' class to execute the main, in main you can find the array variable called myIntArray, customize it.